apiVersion: v1
kind: Service
metadata:
  name: github-pages
  namespace: ingress-nginx
spec:
  type: ExternalName
  externalName: dcruzf.github.io/dcruzf/
  ports:
    - port: 443
      targetPort: 443